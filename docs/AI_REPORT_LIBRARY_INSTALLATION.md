# تثبيت مكتبة التقارير

1. ثبّت الاعتماديات: `pip install -r requirements.txt` (يشمل `python-docx`)
2. تأكد من تشغيل Ollama إن رغبت بمساعدة التصنيف (اختياري للمعالجة الأساسية)
3. افتح `/ai-center` → مكتبة التقارير الذكية

المجلد: `instance/ai_reports/` تحت `data_dir()`
